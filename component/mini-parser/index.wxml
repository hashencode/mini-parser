<view class="mini-parser" wx:if="{{html}}">
  <render wx:if="{{!adaptive || containerWidth>0}}"
          html="{{html}}"
          config="{{config}}"
          containerWidth="{{containerWidth}}">
  </render>
</view>

