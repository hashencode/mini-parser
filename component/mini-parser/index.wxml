<view class="mini-parser" wx:if="{{htmlStr}}">
  <render wx:if="{{containerWidth>0}}"
          htmlStr="{{htmlStr}}"
          config="{{config}}"
          containerWidth="{{containerWidth}}">
  </render>
</view>

